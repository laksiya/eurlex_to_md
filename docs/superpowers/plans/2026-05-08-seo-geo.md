# SEO & GEO Optimisation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add meta tags, Open Graph, JSON-LD structured data, llms.txt, robots.txt, and sitemap.xml so the tool ranks on Google, gets cited by AI assistants, and previews correctly when shared.

**Architecture:** All changes are purely static — edits to `public/index.html`'s `<head>` and three new files in `public/`. No backend changes. No new dependencies. The four files are served as-is by Vercel's static file output.

**Tech Stack:** Plain HTML, JSON-LD (schema.org), llms.txt standard (Answer.AI), XML sitemap

---

## File Map

| Action | File | Responsibility |
|--------|------|---------------|
| Modify | `public/index.html` | Add `<title>`, meta description, robots, canonical, OG, Twitter card, JSON-LD |
| Create | `public/llms.txt` | AI crawler (ChatGPT, Perplexity, Claude) discovery |
| Create | `public/robots.txt` | Crawler permissions + sitemap pointer |
| Create | `public/sitemap.xml` | Single-URL sitemap |

---

## Task 1: Meta tags, Open Graph, Twitter card, and JSON-LD in `index.html`

**Files:**
- Modify: `public/index.html` (inside `<head>`, before `</head>`)

- [ ] **Step 1: Verify the tags don't exist yet**

```bash
grep -c "og:title\|twitter:card\|application/ld+json\|name=\"description\"" public/index.html
```

Expected output: `0`

- [ ] **Step 2: Replace the existing `<title>` tag**

Find this line in `public/index.html`:
```html
  <title>EUR-Lex → Markdown Converter</title>
```

Replace with:
```html
  <title>EUR-Lex to Markdown Converter – Free EU Legal Document Tool</title>
```

- [ ] **Step 3: Add meta + OG + Twitter card tags immediately after the new `<title>` line**

Insert this block after the `<title>` line:
```html
  <meta name="description" content="Convert EU legal documents to clean Markdown. Paste any EUR-Lex URL or CELEX ID — get GDPR, EU AI Act, DORA, DSA, and more as structured Markdown instantly. Free, no login required." />
  <meta name="robots" content="index, follow" />
  <link rel="canonical" href="https://eurlex-to-md.vercel.app/" />

  <!-- Open Graph -->
  <meta property="og:type"        content="website" />
  <meta property="og:url"         content="https://eurlex-to-md.vercel.app/" />
  <meta property="og:title"       content="EUR-Lex to Markdown Converter – Free EU Legal Document Tool" />
  <meta property="og:description" content="Convert EU legal documents to clean Markdown. Paste any EUR-Lex URL or CELEX ID — get GDPR, EU AI Act, DORA, DSA, and more as structured Markdown instantly. Free, no login required." />

  <!-- Twitter Card -->
  <meta name="twitter:card"        content="summary" />
  <meta name="twitter:title"       content="EUR-Lex to Markdown Converter – Free EU Legal Document Tool" />
  <meta name="twitter:description" content="Convert EU legal documents to clean Markdown. Paste any EUR-Lex URL or CELEX ID — get GDPR, EU AI Act, DORA, DSA, and more as structured Markdown instantly. Free, no login required." />
```

- [ ] **Step 4: Add JSON-LD block immediately before `</head>`**

Insert this block just before the closing `</head>` tag:
```html
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "SoftwareApplication",
    "name": "EUR-Lex to Markdown Converter",
    "url": "https://eurlex-to-md.vercel.app",
    "applicationCategory": "Utilities",
    "operatingSystem": "Web",
    "description": "Converts EU legal documents from EUR-Lex into structured Markdown. Supports any CELEX ID — GDPR, EU AI Act, DMA, DSA, DORA, NIS2, and more.",
    "offers": {
      "@type": "Offer",
      "price": "0",
      "priceCurrency": "EUR"
    },
    "about": {
      "@type": "Thing",
      "name": "EU Legislation",
      "description": "Official European Union legislation from EUR-Lex, including regulations and directives such as GDPR, the EU AI Act, the Digital Markets Act, and the Digital Services Act."
    }
  }
  </script>
```

- [ ] **Step 5: Verify all tags are now present**

```bash
grep -c "og:title\|twitter:card\|application/ld+json\|name=\"description\"" public/index.html
```

Expected output: `4`

- [ ] **Step 6: Validate JSON-LD is syntactically valid**

```bash
python3 -c "
import re, json, sys
html = open('public/index.html').read()
m = re.search(r'<script type=\"application/ld\+json\">(.*?)</script>', html, re.DOTALL)
json.loads(m.group(1))
print('JSON-LD valid')
"
```

Expected output: `JSON-LD valid`

- [ ] **Step 7: Commit**

```bash
git add public/index.html
git commit -m "feat: add SEO meta tags, Open Graph, Twitter card, and JSON-LD"
```

---

## Task 2: Create `public/llms.txt`

**Files:**
- Create: `public/llms.txt`

- [ ] **Step 1: Verify file doesn't exist yet**

```bash
test ! -f public/llms.txt && echo "ok" || echo "already exists"
```

Expected output: `ok`

- [ ] **Step 2: Create the file**

Create `public/llms.txt` with this exact content:

```
# EUR-Lex to Markdown Converter

> Free web tool that converts EU legal documents into clean, structured Markdown.
> Input a CELEX ID or EUR-Lex URL; get back Markdown ready to read, download, or process with AI.

## Supported documents

- GDPR (32016R0679) — General Data Protection Regulation
- EU AI Act (32024R1689) — Artificial Intelligence Act
- Digital Markets Act / DMA (32022R1925)
- Digital Services Act / DSA (32022R2065)
- NIS2 Directive (32022L2555)
- EU Data Act (32023R2854)
- Cybersecurity Act (32019R0881)
- DORA (32022R2554) — Digital Operational Resilience Act

## API

GET /generate/{celex_id} — JSON with title, article_count, markdown
GET /health — cache status

## Source

Fetches from the CELLAR linked-data API (Publications Office of the EU).
Built on the eurlex library by K.M.J. Jacobs (MIT).
```

- [ ] **Step 3: Verify content**

```bash
head -3 public/llms.txt
```

Expected output:
```
# EUR-Lex to Markdown Converter

> Free web tool that converts EU legal documents into clean, structured Markdown.
```

- [ ] **Step 4: Commit**

```bash
git add public/llms.txt
git commit -m "feat: add llms.txt for AI crawler discovery"
```

---

## Task 3: Create `public/robots.txt` and `public/sitemap.xml`

**Files:**
- Create: `public/robots.txt`
- Create: `public/sitemap.xml`

- [ ] **Step 1: Create `public/robots.txt`**

```
User-agent: *
Allow: /
Sitemap: https://eurlex-to-md.vercel.app/sitemap.xml
```

- [ ] **Step 2: Create `public/sitemap.xml`**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://eurlex-to-md.vercel.app/</loc>
    <changefreq>monthly</changefreq>
    <priority>1.0</priority>
    <lastmod>2026-05-08</lastmod>
  </url>
</urlset>
```

- [ ] **Step 3: Verify both files**

```bash
grep "Sitemap" public/robots.txt && grep "<loc>" public/sitemap.xml
```

Expected output:
```
Sitemap: https://eurlex-to-md.vercel.app/sitemap.xml
    <loc>https://eurlex-to-md.vercel.app/</loc>
```

- [ ] **Step 4: Commit**

```bash
git add public/robots.txt public/sitemap.xml
git commit -m "feat: add robots.txt and sitemap.xml"
```

---

## Post-Deploy Verification

After deploying to Vercel, manually verify:

- `https://eurlex-to-md.vercel.app/llms.txt` — returns plain text
- `https://eurlex-to-md.vercel.app/robots.txt` — returns `User-agent: *`
- `https://eurlex-to-md.vercel.app/sitemap.xml` — returns valid XML
- Paste the URL into [LinkedIn Post Inspector](https://www.linkedin.com/post-inspector/) to verify OG preview
- Validate JSON-LD at [Google Rich Results Test](https://search.google.com/test/rich-results)
- Submit sitemap in [Google Search Console](https://search.google.com/search-console)
